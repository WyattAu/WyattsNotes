dist/
build/
public/
.cache/
node_modules/
*.min.js
package-lock.json
yarn.lock
pnpm-lock.yaml
.next/
.out/

# Default ignores
**/.git
**/.svn
**/.hg
**/node_modules
**/.next
**/dist

# Custom ignores (examples)
/build
/coverage
yarn.lock
package-lock.json
*.log
*.min.*
.DS_Store

# System files
.DS_Store
Thumbs.db

# Logs and cache
*.log
*.cache

# Sensitive files
.env
.ssl
.pem

# Package manager locks
package-lock.json
yarn.lock
pnpm-lock.yaml

# Config files (plain JS, not TypeScript)
lighthouserc.js

# Playwright E2E tests (handled by Playwright's own config)
e2e/
playwright-report/
test-results/

# Browser extension (separate build context)
browser-extension/

# Mobile app (separate build context)
mobile-app/
