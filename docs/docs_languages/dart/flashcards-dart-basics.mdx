---
title: 'Dart Flashcards: Fundamentals'
slug: flashcards-dart-basics
description:
  '20 interactive flashcards covering Dart type system, OOP, async, Dart 3 features, collections,
  error handling, Flutter basics, state management, and testing.'
tags:
  - Dart
  - Flashcards
categories:
  - Dart
---

import FlashcardDeck from '@site/src/components/interactive/FlashcardDeck';

## Dart -- Fundamentals Flashcards

20 interactive flashcards covering core Dart concepts from null safety to Dart 3 features and
Flutter basics. Press Space to flip, rate 1-4.

---

export const flashcardData = [{id:"dart-null-safety-001",front:"What is Sound Null Safety in Dart and what guarantee does it provide?",back:"Sound null safety is a compile-time guarantee that null values cannot appear where a non-nullable type is expected. The Dart compiler performs static analysis and flow analysis to ensure that nullable types (T?) are explicitly handled before use. If the compiler cannot prove a variable is non-null, it rejects the code. This eliminates null-reference exceptions at runtime without requiring runtime checks.",tags:["dart","null-safety","types"],difficulty:"medium"},{id:"dart-late-keyword-002",front:"What does the `late` keyword do and when should you use it?",back:"`late` defers initialization of a non-nullable variable to a later point. Use it when: 1) The variable is initialized in the constructor body (not initializer list), 2) It is set by a method before being read, 3) It holds a lazily computed value. The compiler trusts you that it will be non-null when accessed -- if it is not, a runtime LateInitializationError occurs.",tags:["dart","null-safety","variables"],difficulty:"easy"},{id:"dart-required-named-params-003",front:"What is the `required` keyword used for in Dart?",back:"`required` marks named parameters as mandatory when calling a function or constructor. Example: `void configure({required String host, int port = 8080}) {}`. Callers must provide `host` but `port` has a default. It was introduced with null safety to replace `@required` from the meta package.",tags:["dart","functions","syntax"],difficulty:"easy"},{id:"dart-mixins-004",front:"What is a mixin in Dart and how does it differ from inheritance?",back:"A mixin is a class-like construct that provides reusable method implementations to other classes through the `with` clause. Unlike inheritance (single `extends`), a class can use multiple mixins. Mixins cannot be instantiated directly and do not have their own constructors. They enable a form of multiple inheritance for method reuse without the diamond problem. Example: `class Bird extends Animal with Flyer, Walker {}`.",tags:["dart","oop","mixins"],difficulty:"medium"},{id:"dart-extension-methods-005",front:"What are extension methods in Dart?",back:"Extensions add new methods to existing types (including classes you do not own) without subclassing. Example: `extension StringX on String { int get asInt => int.parse(this); }`. They are resolved at compile time and do not modify the original class. Extensions can also add getters, operators, and static members. They are ideal for utility methods.",tags:["dart","oop","extensions"],difficulty:"medium"},{id:"dart-factory-constructors-006",front:"What is a factory constructor in Dart and how does it differ from a generative constructor?",back:"A factory constructor (keyword `factory`) does not always create a new instance. It can return: 1) An existing cached instance (singleton pattern), 2) A subtype instance, 3) An instance from a different source. Unlike generative constructors, factory constructors cannot use initializer lists and must explicitly return an instance with `return`. Example: `factory Logger(String name) => _cache.putIfAbsent(name, () => Logger._internal(name));`.",tags:["dart","oop","constructors"],difficulty:"medium"},{id:"dart-future-async-await-007",front:"How do `Future`, `async`, and `await` work together in Dart?",back:"A `Future<T>` represents a value or error that will be available later. Marking a function `async` makes it return a Future automatically. `await` pauses the function until the Future completes, yielding the result or throwing the error. `async/await` is syntactic sugar over the Future API (`.then()`, `.catchError()`, `.whenComplete()`). They enable writing asynchronous code that reads like synchronous code.",tags:["dart","async","futures"],difficulty:"easy"},{id:"dart-stream-008",front:"What is a Stream in Dart and how does it differ from a Future?",back:"A Stream delivers a sequence of values or errors over time (like an async Iterable), while a Future delivers a single value or error. Streams can be single-subscription (one listener, events consumed once) or broadcast (multiple listeners, each gets events from subscription point). Listen with `stream.listen()`, transform with `stream.map()`, and cancel with `subscription.cancel()`.",tags:["dart","async","streams"],difficulty:"medium"},{id:"dart-completer-009",front:"What is a `Completer<T>` and when would you use one?",back:"A Completer wraps a Future and gives you manual control over its completion. Call `completer.complete(value)` to resolve it or `completer.completeError(error)` to reject it. Use it when you need to bridge callback-based APIs with Future-based code, or when the Future must be completed from a different scope. Access the underlying Future via `completer.future`.",tags:["dart","async","completer"],difficulty:"medium"},{id:"dart-sealed-classes-010",front:"What are sealed classes in Dart 3 and why are they useful?",back:"Sealed classes restrict which types can extend or implement them to a known, finite set defined in the same library. The compiler enforces exhaustiveness checking in switch expressions -- if you handle all subtypes, no `default` case is needed. Example: `sealed class Shape { } class Circle extends Shape { } class Square extends Shape { }`. Switching on `Shape` without a default is exhaustive and safe.",tags:["dart","dart3","sealed-classes"],difficulty:"medium"},{id:"dart-pattern-matching-011",front:"What pattern matching features did Dart 3 introduce?",back:"Dart 3 added: 1) Object and type test patterns (`if (obj is String s)`), 2) Logical patterns (`||`, `&&`, `!`), 3) Relational patterns (`case > 0`), 4) Cast patterns (`case num as int i`), 5) List and map patterns (`case [a, b, ...rest]`), 6) Variable binding in `if-case` and `switch-case`, 7) Null-check and null-assert patterns. These make data extraction from complex structures concise and safe.",tags:["dart","dart3","pattern-matching"],difficulty:"medium"},{id:"dart-records-012",front:"What are records in Dart 3 and how do they work?",back:"Records are anonymous, immutable, aggregate data types. Syntax: `var record = ('Alice', age: 30)` creates a positional record with a named field. Access with `record.$1` (positional) or `record.age` (named). Records have structural equality -- two records with the same fields are equal. They support destructuring: `var (name, age: a) = record;`. Records are value types, perfect for returning multiple values from functions.",tags:["dart","dart3","records"],difficulty:"easy"},{id:"dart-class-modifiers-013",front:"What class modifiers did Dart 3 introduce?",back:"Dart 3 added: `sealed` (restricts subtyping to same library), `final` (prevents being extended or implemented outside own library), `interface` (class can only be implemented, not extended), `base` (ensures all subtypes also use `base`, enabling private field access guarantees), and `mixin class` (usable as both a class and a mixin). These replace informal conventions with compiler-enforced restrictions.",tags:["dart","dart3","class-modifiers"],difficulty:"hard"},{id:"dart-collections-014",front:"What are Dart's core collection types and their key characteristics?",back:"List (ordered, indexed,
growable or fixed-length): `[1, 2, 3]`. Set (unordered, unique elements, hash-based): `{1, 2, 3}`.
Map (key-value pairs, keys are unique): `{'a': 1}`. All implement Iterable, supporting `map`,
`where`, `reduce`, `fold`, `any`, `every`, etc. Dart also provides Queue, LinkedList, and
specialized types through the collection and dart:collection
library.",tags:["dart","collections"],difficulty:"easy"},{id:"dart-iterable-015",front:"What is the
`Iterable` interface in Dart?",back:"Iterable is the lazy base class for all collections. It
provides a read-only view via an Iterator without materializing the full collection. Key methods:
`map()`, `where()`, `expand()`, `reduce()`, `fold()`, `take()`, `skip()`, `any()`, `every()`,
`firstWhere()`, `singleWhere()`. Iterables are chainable -- each operation returns a new lazy
Iterable. Call `.toList()` or `.toSet()` to
materialize.",tags:["dart","collections","iterable"],difficulty:"medium"},{id:"dart-exception-vs-error-016",front:"What
is the difference between `Exception` and `Error` in Dart?",back:"Exception represents conditions
that a program should anticipate and handle (IOException, FormatException). Error represents
programming bugs that should not be caught (StateError, TypeError, ArgumentError). Catching Error is
discouraged -- it hides bugs. Use `on Exception catch (e)` to catch only exceptions, or `catch (e)`
to catch everything. Dart does not have checked exceptions like
Java.",tags:["dart","error-handling"],difficulty:"easy"},{id:"dart-try-catch-finally-017",front:"How
does `try-catch-finally` work in Dart?",back:"`try` wraps code that may throw. `on Type catch (e)`
catches a specific type. `catch (e, stackTrace)` catches everything and provides the stack trace.
`finally` always executes (cleanup code) regardless of whether an exception was thrown or caught.
You can combine:
`try { ... } on FormatException catch (e) { ... } catch (e) { ... } finally { ... }`. Re-throw with
`rethrow` to
propagate.",tags:["dart","error-handling"],difficulty:"easy"},{id:"dart-statelesswidget-vs-statefulwidget-018",front:"What
is the difference between StatelessWidget and StatefulWidget in Flutter?",back:"StatelessWidget
describes a part of the UI that depends only on its configuration (props). Its `build()` method is
called with an `ImmutableWidget` context and returns a widget tree. It cannot change after being
built. StatefulWidget holds mutable state via a `State` object. When state changes, call
`setState()` to trigger a rebuild. The `createState()` method returns a State instance that persists
across rebuilds and has access to `context` and lifecycle
methods.",tags:["dart","flutter","widgets"],difficulty:"medium"},{id:"dart-state-management-019",front:"What
are the common state management patterns in Dart/Flutter?",back:"1) setState: Built-in, simple,
scoped to one widget. 2) InheritedWidget / Provider: Propagate state down the tree via context. 3)
Riverpod: Compile-safe, testable, no BuildContext dependency. 4) BLoC/Cubit: Events/states with
streams, separates business logic from UI. 5) GetX: Lightweight, uses observables. 6) Redux: Single
store, pure reducers, predictable. Choice depends on app complexity, team size, and testability
requirements.",tags:["dart","flutter","state-management"],difficulty:"medium"},{id:"dart-testing-020",front:"What
are the three levels of testing in Dart/Flutter and what tools are used?",back:"1) Unit tests: Test
individual functions and classes in isolation. Use `test` package and `test()` function. Mock
dependencies with `mockito` or `mocktail`. 2) Widget tests: Test individual UI widgets in a test
environment. Use `flutter_test` with `testWidgets()` and `WidgetTester` to pump widgets, tap, enter
text, and verify output. 3) Integration tests: Test the full app end-to-end on a real
device/emulator. Use `integration_test` package with `testWidgets()` driving the actual app. Run
with `flutter test integration_test/`.",tags:["dart","testing","flutter"],difficulty:"easy"}];  <FlashcardDeck deckId="dart-basics" title="Dart -- Fundamentals" description="20 interactive flashcards covering type system, OOP, async, Dart 3 features, collections, error handling, Flutter, state management, and testing." cards={flashcardData} /> 